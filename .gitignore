# Credentials & secrets
.credentials.json
.credentials/

# Debug logs
debug/

# Cache
cache/

# Session data
projects/
tasks/
teams/

# MCP config (may contain tokens)
agents/.mcp.json

# Misc
statsig/
*.lock

# History & logs
history.jsonl
file-history/
transcripts/
shell-snapshots/

# Local settings & state
# settings.json — tracked unconditionally (statusLine, env, hooks, permissions)
settings.local.json
stats-cache.json
paste-cache/
telemetry/
todos/
plans/
!docs/superpowers/plans/
!docs/superpowers/specs/
/plugins/
!/plugins/
/plugins/*
!/plugins/matt-loop/
!/plugins/matt-loop/**
!/plugins/auto-loop/
!/plugins/auto-loop/**

# Runtime & OMC state
.omc/
.omc-config.json
.session-stats.json
mcp-needs-auth-cache.json
# hud/ — tracked unconditionally (statusLine HUD scripts)
ide/
remote/
backups/
downloads/
session-env/
.claude/

# gstack (installed via setup, not tracked)
skills/gstack
skills/gstack-*

# Local state
.backups/
homunculus/
mcp-health-cache.json
metrics/
sessions/

# Backups & research (local only)
*.backup.*
research/
skills/learned/
skills/skill-stocktake/

# eval scratch (skill-creator workspaces)
skills/workflow-plan-workspace/
# vendored gstack browse: exclude heavy test fixtures (28M) from harness release
skills/browse/test/
