# Diet inventory — autocode SKILL.md (before: 634 lines)

| # | Item (rule / step / red flag) | Before (line) | After (where it lives now) |
|---|---|---|---|
| 1 | Intro: three ideas (hypotheses not experiments; parallel where safe, serial measurement; route by difficulty) | 9-20 | SKILL.md 9-20 (unchanged) |
| 2 | Intro: board via $loop-report (3I), gates via $loop-gates (2E); missing shared skill → say `weed-harness 3.x required: <skill> missing` once, use its fallback | 22-28 | SKILL.md 22-27 (unchanged) |
| 3 | Subcommands table (init N default 20 / run --parallel --on / status / resume; confirmation column) | 30-37 | SKILL.md 32-39 (unchanged) |
| 4 | Step 0 paths (all env vars incl. REPORT_DIR) | 39-53 | SKILL.md 41-47 (collapsed to prose, same vars) + reference.md § File structure (258-) |
| 5 | Step 1 parse: init N = max_experiments; run --parallel (max 4) / --on override this run only; status; resume | 55-60 | SKILL.md 49-54 (unchanged) |
| 6 | 2A reconnaissance before questions | 66-71 | SKILL.md 60-65 (unchanged) |
| 7 | 2B interview: AskUserQuestion one at a time, recon default recommended; 11-field table; 5 follow-ups (screen_command) | 73-95 | SKILL.md 67-77 (procedure + field list) + reference.md § Interview fields (6-28, table + follow-ups verbatim) |
| 8 | 2C difficulty: standard→Deep, hard→Max; 5 "hard" criteria; shown in approval for override; can escalate at run time | 97-108 | SKILL.md 79-90 (unchanged) |
| 9 | 2D program.md template | 110-156 | reference.md § program.md (30-76, verbatim); pointer at SKILL.md 92-95 |
| 10 | 2D: results.tsv header; create Step 0 dirs; add `.autocode/` to .gitignore (ask first) | 158-161 | SKILL.md 96-98 (unchanged) |
| 11 | 2E gates: follow $loop-gates; locate once; ask install once, decline → unlazy_gates false; GATES.md + verify-guard/metric/target scripts (portable Node, no deps, exact pass strings, metric re-measures never trusts); thresholds from program.md/state.json so CHECK lines don't change; show user GATES.md + scripts; approve once with consent; re-verify at 3F; retry bound / handoff / Orca boundaries are loop-gates'; no Stop hook | 163-178 | SKILL.md 100-115 (unchanged) |
| 12 | 2F approval: AskUserQuestion [Approve and save] [Edit and regenerate] [Start over] | 180-183 | SKILL.md 117-120 (unchanged) |
| 13 | Step 3 coordinator role: owns scheduling, measurement, keep/discard, merging, state, strategist conversation; never edits target code; never reasons what to try | 189-191 | SKILL.md 126-128 (unchanged) |
| 14 | 3A pre-flight 1-6: program/targets/clean tree; experiment branch autocode/{stamp}, best = its head; guard on unmodified; resolve routing, Orca probe only when --on or parallel>1 + user asked (status/run-list, orca-ide fallback), local subagents default not degraded; load lessons; probe report delivery and print route | 193-208 | SKILL.md 130-144; step 6 now `deliver.py probe --page …` + note that the page need not exist yet |
| 15 | 3B baseline: metric ×3 serial, finite check, baseline=median, noise_band=max dev floored 0.5%; only keep rule; write state.json; display + first publish (running, metric strip, empty frontier); every state change republishes | 210-245 | SKILL.md 146-159 (rules unchanged); state.json example → reference.md § state.json (78-97) with pointer + key list at 153-155 |
| 16 | 3C strategist: one per run on program tier; keep agent id; SendMessage same conversation, no respawn; first prompt contents; initial frontier ≥ 2×parallel disjoint; strategist brief verbatim; hypothesis schema; status transition chain; reply schema | 247-302 | SKILL.md 161-175 (spawn/persist/first-prompt/initial-frontier/status chain/delta kinds); brief → reference.md § Strategist brief (135-149), schema → § Hypothesis (99-119), reply → § Strategist reply (121-133) |
| 17 | 3D scheduler: state.json atomic on every change; results.tsv append; republish on every state change (dispatch, measurement, keep/discard, delta, plateau/escalation) not timer/commit; FILL/WAIT/MEASURE/DECIDE/FEED/TERMINATE pseudo (touches intersection skip, wait for first completion); empty frontier → refill once, nothing twice → exhausted | 304-327 | SKILL.md 177-200 (unchanged) |
| 18 | 3D-1 dispatch: worktree add from best_commit; worktree_setup (note duration, mention in summary if dominates); route by difficulty, spawn in background with verbatim prompt (edit only touches, guard retry ≤2, commit msg, result.json fields, never run metric/merge/other worktrees/hooks); Orca placement (run-create once, task-create, worker-start flags from model-routing, lifecycle lines, worker_done is a signal not evidence); status running | 329-354 | SKILL.md 202-220 (worktree, background spawn, Orca placement, worker_done, status running; prompt constraints summarised 206-212); verbatim prompt → reference.md § Experimenter prompt (151-165) |
| 19 | 3D-2 measure: coordinator only, serial; crash → skip; beyond_scope → re-dispatch once next route up, deep beyond_scope → discard, not counted; metric in worktree to logs, parse last line, non-finite crash; screen_command first, screened discard; nothing else runs metric | 356-367 | SKILL.md 222-233 (unchanged) |
| 20 | 3D-3 decide+merge pseudo (noise band, --no-ff, conflict abort, re-measure merged, interaction reset); append results.tsv; lesson file {iteration,type,description,action,tags}; worktree remove + branch -D; experiments_done++; conflict/interaction informative to strategist | 369-392 | SKILL.md 235-256 (unchanged) |
| 21 | 3D-4 feedback: single message (verbatim); apply delta (files, cancel, running cancelled left to finish, priorities); back to FILL immediately | 394-406 | SKILL.md 258-267 (apply-delta rules unchanged); verbatim message → reference.md § Result message (167-175) |
| 22 | 3E plateau: ≥5 discards or no keep in 8; retrospective file contents; deep→max once (contents, replace id, escalated=true, fresh frontier); on max final frontier → plateau | 408-419 | SKILL.md 269-280 (unchanged) |
| 23 | 3F termination: 4 conditions; let running finish; cleanup worktrees/Orca/checkout best; unlazy --reverify first, summary only on ALL MET else ABANDON + handoff; target_reached backed by gate; final publish done/terminatedReason/outcome (numstat, name-status, Korean line per file, .autocode excluded); final summary template | 421-465 | SKILL.md 282-313 (conditions, cleanup, reverify/ALL MET/ABANDON, final publish unchanged); NEW gates_caught rule 298-300; summary template → reference.md § Final summary (216-240, + `Gates caught` line) with pointer 309-313 |
| 24 | 3G failure: timeout 30 min or setup+guard×3 → crash/timeout; worktree recreate once then discard/worktree; metric fails after merge → revert + pause | 467-475 | SKILL.md 315-323 (unchanged) |
| 25 | 3H routing: read $model-routing, don't restate; role→tier table; strategist assigns difficulty; beyond_scope reroute + `rerouted`; Deep ceiling for experimenters; Orca flags from model-routing; no-agent platform fallback, say so once | 477-498 | SKILL.md 325-346 (unchanged) |
| 26 | 3I board: owner split (autocode data, loop-report page+route); never run orca artifacts/tab/reload here; publish = data.json + render.py; delivery keeps delivery.json; publish timing (3B, 3D, 3E, 3F); Data keys JSON + 4 bullets (hypotheses mirror, Korean translation, terminatedReason/validate, progress.current/blockers, page derives) | 500-557 | SKILL.md 348-379: owner split, publish = render.py + `deliver.py publish`, relay JSON answer, probe/show verbs, publish timing; delivery.json never read by autocode; Data JSON + 4 bullets → reference.md § Board data (177-214) with pointer 375-379 |
| 27 | Step 4 status: read state/results/hypotheses; status template | 561-577 | SKILL.md 381-385 (reads + display); template → reference.md § Status (242-256), Board line now from `deliver.py show` |
| 28 | Step 5 resume 1-4 | 579-589 | SKILL.md 388-398 (unchanged) |
| 29 | File structure tree | 593-613 | reference.md § File structure (258-278); pointer at SKILL.md 47 |
| 30 | Anti-patterns ×9 | 615-634 | SKILL.md 402-420 (9 items, one line each; the two board items now name `deliver.py` instead of orca artifacts/tab/reload) |

After: SKILL.md 420 lines (target 350 not reached by moving reference material; remaining lines are procedure — stopped per coordinator instruction), reference.md 278 lines. Beyond the spec's list, the interview table and the three verbatim prompts (strategist brief, experimenter prompt, result message) were also moved to reference.md; their rules remain summarised in SKILL.md at the same step.
